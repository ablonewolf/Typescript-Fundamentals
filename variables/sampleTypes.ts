let found = true;
let grade = 90;
let firstName = 'Arka';
let lastName = 'Bhuiyan';

if (found) {
  console.log(`The grade is ${grade}`);
  console.log(`Hello ${firstName} ${lastName}`);
}
console.log(`Type of variable found is: ${typeof found}`);
console.log(`Type of variable grade is: ${typeof grade}`);
console.log(
  `Type of variables firstName and lastName are: ${typeof firstName} and ${typeof lastName}`
);
